server:
  port: 8080
spring:
  application:
    #应用的名称，可选
    name: reggie_take_gyq
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/reggie
      username: root
      password: gyq19980901
  redis:
    host: localhost
    port: 6379
    #password: 123456
    database: 0
  cache:
    redis:
      time-to-live: 1800000 #设置缓存有效期
    jedis:
      pool:
        max-active: 8  #最大连接数
        max-wait: 1ms  #连接池最大阻塞等待时间
        max-idle: 4    #连接池中最大空闲时间
        min-idle: 0    #连接池中的最小空闲连接

mybatis-plus:
  configuration:
    #在映射实体或者属性时，将数据库中表名和字段名中的下划线去掉，按照驼峰命名法映射
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: ASSIGN_ID
reggie:
  path: D:\img\


# AccessKey ID   LTAI5t9AHxMN7ku3KaKUVAMa

# AccessKey Secret   0ulSO2pfLHQNdSSguvnKTC1mBfQyUV